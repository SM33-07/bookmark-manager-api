# Copy this file to .env and .env.test, adjusting values as needed.

# PostgreSQL connection string (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://bookmark_user:bookmark_pass@localhost:5432/bookmark_manager?schema=public"

# Server port
PORT=4000
